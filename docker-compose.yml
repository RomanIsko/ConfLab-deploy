web:
  build: .
  volumes:
   - .:/app
  ports:
   - 8080:8080
  links:
   - db
db:
  image: postgres
  volumes:
   - ./db_data:/var/lib/postgresql/data
  environment:
    POSTGRES_PASSWORD: Passw0rd
    POSTGRES_DB: conflab